# To-Do Tasks

- [ ] Create Swift/SwiftUI iOS project structure for `fen` with `llama.cpp` Metal bindings & ZIM reader integration.
- [ ] Verify `fen` app pipeline and update workspace tracking files.
