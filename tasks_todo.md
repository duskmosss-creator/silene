# To-Do Tasks

- [ ] Wait for user review and approval of the `fen` implementation plan (`implementation_plan.md`).
- [ ] Develop Python prototype for ZIM parsing + vector embedding RAG pipeline (`fen_core.py`).
- [ ] Create Swift/SwiftUI iOS project structure for `fen` with `llama.cpp` Metal bindings & ZIM reader integration.
- [ ] Verify `fen` app pipeline and update workspace tracking files.
